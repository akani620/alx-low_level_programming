#include <stdio.h>

/*
 * main- main block prints a string to standard output
 *
 * returns (0) if successful
 *
 */

int main(void)
	{
		printf("with proper grammar, but the outcome is a piece of art,\n");	
		return (0);
	}
