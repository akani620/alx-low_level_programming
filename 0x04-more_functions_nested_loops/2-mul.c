#include "main.h"

/**
 * mul - multiply 2 integers
 * @a: int type number
 * @b: int type number
 * Return: result of multiplication
 */

int mul(int a, int b)
{	
	if ((a <= 0 || a > 0) && (b <= 0 || b > 0))
	return (a * b);
}
